a.enterText()
ElementUtil sendkeys > clear the value before entering
 public void doSendKeys(By locator, String value) {
        WebElement ele = getElement(locator);
        ele.clear();
        ele.sendKeys(value);
    }
-----------------------------------------------------------